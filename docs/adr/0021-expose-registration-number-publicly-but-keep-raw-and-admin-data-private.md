# Expose Registration Number publicly but keep raw and admin data private

Registration Number may be included in Public Listing Data when it is visible in
the Source. VIN, Raw Listing Data, crawler internals, parser errors, and admin
operations remain behind the Admin Password Gate.
