# Start public pages as noindex

Public analytics and Listing pages will be accessible from the first version but
should start with `noindex` until Indexing Readiness is reached. Early crawl
coverage, parser quality, URL structure, and metadata are expected to evolve, so
search indexing should be enabled as a deliberate later rollout.
