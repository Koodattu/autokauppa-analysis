# Keep the public API product-facing

The public API will support the web product's analytics, listing search, Public
Listing Pages, and filter metadata, but it will not be a general open data API
in the first version. Bulk exports, unbounded listing dumps, Raw Listing Data,
and crawler/admin operations stay out of unauthenticated public access.
