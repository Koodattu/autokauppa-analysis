# Use Listing as the canonical entity

We will model the primary tracked object as a Listing rather than a Vehicle. The
system observes marketplace advertisements and their changes over time; reliably
identifying the underlying physical vehicle across relists, duplicates, missing
identifiers, or seller behavior is a separate and harder problem that should not
drive the initial model.
