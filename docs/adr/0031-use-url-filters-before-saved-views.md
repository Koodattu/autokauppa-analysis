# Use URL Filters before Saved Views

The first version will represent Analysis Queries as URL Filters rather than
persisted Saved Views, watchlists, or alerts. Public analytics should be
shareable without user accounts, and saved user-owned state can be added later
if the product needs it.
