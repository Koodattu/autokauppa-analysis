# Use Registration Number as a matching signal, not Listing identity

Registration Number may be captured from Detail Page Data when visible and used
for Specific-Car Lookup, possible relist detection, and private analysis. It
must not be used as the primary Listing identity because it can be missing,
hidden, mistyped, privacy-sensitive, or shared across multiple Listings over
time.
