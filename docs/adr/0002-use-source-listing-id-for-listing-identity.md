# Use Source Listing ID for Listing identity

Listings are identified by the pair of Source and Source Listing ID. Nettiauto
search result markup exposes a stable numeric listing/ad ID in listing URLs and
card metadata, while VIN and vehicle attributes may be missing, invalid, or
shared across relists; fuzzy vehicle matching can be added later without making
it the initial identity rule.
