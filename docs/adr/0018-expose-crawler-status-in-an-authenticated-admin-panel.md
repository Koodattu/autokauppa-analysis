# Expose Crawler Status in an authenticated Admin Panel

Crawler Status should be visible through an authenticated Admin Panel rather
than hidden in logs or exposed publicly. The Market Dataset depends on slow and
imperfect crawl coverage, so private operational visibility is necessary to
interpret analytics freshness and failures correctly.
